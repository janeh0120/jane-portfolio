---
import { site } from '../data/site';

const base = import.meta.env.BASE_URL;
const root = base.endsWith('/') ? base.slice(0, -1) : base;
const pathname = Astro.url.pathname.replace(/\/$/, '') || '/';

function resolveHref(href: string) {
  if (href.startsWith('/#')) return `${root}${href}`;
  if (href.startsWith('/')) return `${root}${href}`;
  return href;
}

function isActive(href: string) {
  if (href === '/about') return pathname.endsWith('/about');
  if (href === '/extra') return pathname.endsWith('/extra');
  if (href === '/history') return pathname.endsWith('/history');
  if (href === '/#projects') {
    return pathname === '/' || pathname === root;
  }
  return false;
}
---

<header class="sticky top-0 z-50 bg-white/70 backdrop-blur-sm">
  <div
    class="relative mx-auto flex max-w-[1400px] items-center justify-between gap-6 px-6 py-5 md:px-10"
  >
    <a
      href={base}
      class="nav-logo inline-flex items-center transition hover:opacity-70"
      aria-label={`${site.name} home`}
    >
      <span
        class="nav-logo-mark block h-7 w-7 shrink-0 bg-ink"
        style={`mask: url('${base}${site.logo}') center / contain no-repeat; -webkit-mask: url('${base}${site.logo}') center / contain no-repeat;`}
      ></span>
    </a>
    <nav class="main-nav flex items-center gap-5 md:gap-8" aria-label="Main" data-comment-id="main-nav">
      {
        site.nav.map((item) => (
          <a
            href={resolveHref(item.href)}
            class:list={[
              'text-sm font-medium transition hover:opacity-70',
              isActive(item.href) && 'underline decoration-ink/30 underline-offset-4',
            ]}
          >
            {item.label}
          </a>
        ))
      }
    </nav>
    <div id="comment-toggle-anchor" class="comment-toggle-anchor" data-comment-ui></div>
  </div>
</header>
